# Cloudflare Access (optional / not preferred)

For the current pause, prefer **[PRIVATE_ACCESS.md](./PRIVATE_ACCESS.md)**:

- Public → maintenance page
- You + counsel → `?access=SECRET` unlock link

Cloudflare Access on `eb5base.com` shows a login wall to **everyone**, which is not what we want while the branded pause page should stay public.

Only use Access if you intentionally want every visitor to hit Cloudflare’s gate.
